spring.application.name=Nidar
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# msg91.auth-key=509611AT7FGtbBD69e2851aP1
# msg91.sender-id=NIDAR
# msg91.route=4
# msg91.template-id=123456
# vault.uri=http://localhost:8200
# vault.token=nidar_vault_token
# minio.url=http://localhost:9000
# minio.access-key=nidar_minio_user
# minio.secret-key=nidar_minio_password
# minio.bucket-name=nidar-evidence
jwt.secret=${JWT_SECRET}
jwt.vault-secret=${JWT_VAULT_SECRET}
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
server.port=${PORT:8080}
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
# spring.data.redis.password=nidar_redis_pass
# spring.data.redis.timeout=2000ms
# logging.level.org.springframework.security=DEBUG
logging.level.org.springframework=INFO
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# firebase.service-account-path=nidar-firebase-adminsdk.json
# firebase.database-url=https://nidar-default-rtdb.firebaseio.com